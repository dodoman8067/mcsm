#ifndef __MCSM_ALGORITHM_UTILS_H__
#define __MCSM_ALGORITHM_UTILS_H__

#include <vector>
#include <algorithm>

namespace mcsm {
    template<typename T>
    bool compare(std::vector<T>& v1, std::vector<T>& v2);
}

#endif // __MCSM_ALGORITH_UTILS_H__