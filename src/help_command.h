#ifndef __HELP_COMMAND_H__
#define __HELP_COMMAND_H__

#include "command.h"

namespace mcsm {
    class HelpCommand : public mcsm::Command {
    public:
        HelpCommand();
        ~HelpCommand();

    private:

    };
}

#endif